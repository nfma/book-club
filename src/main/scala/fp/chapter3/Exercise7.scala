package fp.chapter3

object Exercise7 {
  println("Yes, it can; Because a product of anything by 0.0 is 0.0")
  println("You would immediately stop recursing till the end of the list but instead return 0.0 as the final result of processing the rest of the list, and calculate the result given the pending calculations")
}
