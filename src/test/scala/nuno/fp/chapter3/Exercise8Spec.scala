package nuno.fp.chapter3

import org.scalatest.WordSpec

class Exercise8Spec extends WordSpec {
  "Exercise 8" should {
    "answer question" in {
      println("foldRight is the computation that uses the data constructors to build a List (just like apply did)")
    }
  }
}
